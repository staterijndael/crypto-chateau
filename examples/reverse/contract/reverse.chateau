package endpoints

service Reverse {
    Stream ReverseMagicString(ReverseMagicStringRequest req) -> (ReverseMagicStringResponse)
}

object ReverseMagicStringRequest {
    string magicString
}

object ReverseMagicStringResponse {
    string reversedMagicString
}